// Iterate over "array1" $ log the values using for of loop

// Using a `for...of` loop to iterate over an array.
// 'element' represents each item in the 'array1' array.
const array1 = ["a", "b", "c"];

for (const element of array1) {
  console.log(element); // Output: a, b, c
}
